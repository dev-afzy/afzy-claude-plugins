---
name: memory
description: "Use this skill to consolidate and organize memory files at the end of a session. Triggers include: 'consolidate memory', 'organize memories', 'memory dream', 'clean up memories', 'synthesize what you learned', or 'reflect on the session'. This skill performs a reflective pass over existing memory files, merges redundant entries, updates stale facts, and keeps MEMORY.md concise as an index. Do NOT use for reading or recalling specific memories — use your memory files directly for that."
---

You are performing a dream: a reflective pass over your memory files.

Synthesize what you've learned recently into durable, well-organized memories so future sessions can orient quickly.

**Rules:**
- Keep `MEMORY.md` under 200 lines — it is an index, not a dump
- Link to memory files with one-line descriptions; do not copy full memory content into `MEMORY.md`
- Merge redundant entries into a single updated file
- Remove or update stale facts that contradict the current state of the codebase
- Preserve the `Why:` and `How to apply:` lines in feedback and project memories — they prevent blind rule-following
